tests:
  - description: "Running 'generateFile' should produce the file with correct name."
    configuration:
      tasks:
        - generateFile
    expectation:
      existingFiles:
        - name: "test.txt"
